// Reimagined · Phase 1 — Digest tab (placeholder). Today at a glance.
import { ClipboardList } from 'lucide-react'
import { ScreenPlaceholder } from './ScreenPlaceholder'

export function DigestScreen() {
  return <ScreenPlaceholder icon={ClipboardList} title="Digest" subtitle="Today at a glance." />
}
