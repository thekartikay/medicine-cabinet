// Reimagined · Phase 1 — Restock tab (placeholder). What to reorder.
import { ShoppingCart } from 'lucide-react'
import { ScreenPlaceholder } from './ScreenPlaceholder'

export function RestockScreen() {
  return <ScreenPlaceholder icon={ShoppingCart} title="Restock" subtitle="What to reorder." />
}
