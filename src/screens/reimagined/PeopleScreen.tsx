// Reimagined · Phase 1 — People tab (placeholder). The people Priya cares for.
import { Users } from 'lucide-react'
import { ScreenPlaceholder } from './ScreenPlaceholder'

export function PeopleScreen() {
  return <ScreenPlaceholder icon={Users} title="People" subtitle="The people you care for." />
}
