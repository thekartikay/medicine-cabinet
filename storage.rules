rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TODO: tighten rules before production
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
