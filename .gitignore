# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
functions/node_modules/
dist
functions/lib/
dist-ssr
*.local

# Environment — never commit secrets
.env
.env.local
.env.*.local

# Firebase service account (used by seed scripts / admin SDK)
service-account.json
*service-account*.json

# Vite cache
.vite/

# Firebase emulator export (regenerated on emulators:export — don't commit)
emulator-data/

# Capacitor
android/
ios/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
CLAUDE.md

# Claude Code per-user settings
.claude/settings.local.json

# Design / spec PDFs — large render artifacts, regenerated from source
Documents/*.pdf

# Local document scratch dir
Documents/
